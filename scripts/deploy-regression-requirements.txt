# Minimal dependencies for the isolated deploy/browser gate; no running app or database needed.
pytest==8.3.5
httpx==0.28.1
websockets==16.0
Jinja2==3.1.6
PyYAML==6.0.3

# Calendar move API checks use the real router with isolated auth/storage boundaries.
fastapi==0.141.1
pydantic==2.13.4
SQLAlchemy==2.0.50
python-multipart==0.0.32

# Media cache/GPU checks import the real media service and its encrypted store.
cryptography==50.0.1
Pillow==12.3.0
