"""Vendored pure-python Nostr core (bech32, bip340 schnorr, nip44, event) for the standalone
pcnode agent — copied verbatim from app/services/nostr/ so the agent runs with no PosterChanAI install."""
