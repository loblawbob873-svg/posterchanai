# Core dependencies
requests>=2.31.0
# No FUSE required! Pure Python implementation

# Note: tkinter is optional and not installable via pip
# It's a system package that comes with Python (when compiled with tk support)
# The setup wizard will use CLI mode if tkinter is not available

# Note: FUSE is required for filesystem mounting
# Install as a system package:
#   - Gentoo: emerge sys-fs/fuse
#   - Debian/Ubuntu: apt install fuse3 python3-fuse
#   - Arch: pacman -S fuse python-fusepy
#   - Fedora: dnf install fuse fuse-python
