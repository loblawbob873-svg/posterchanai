# Application
SECRET_KEY=change-this-to-a-secure-random-key
DATABASE_URL=sqlite:///./posterchanai.db

# Note: AI settings are stored in the database and configured via admin panel
# Default settings will be created on first run
