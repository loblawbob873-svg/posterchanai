# Image-generation dependencies (Intel Arc / PyTorch XPU). Install into the image venv
# (venv-xpu-new) IN ADDITION to requirements.txt.
#
# MODERN STACK (2026-06): native PyTorch XPU - no IPEX needed for diffusers.
#   1) Install torch 2.8 XPU FIRST (it bundles its own oneAPI 2025.1 runtime via pip):
#        pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/xpu
#   2) Then this file.
#
# SYSTEM REQUIREMENT: Intel Graphics Compiler (IGC) >= 2.35.5 (run scripts/install-igc.sh).
#   - SDXL at >=768x768 FAILS on older IGC (2.23.0 / 2.35.2) with oneDNN
#     "could not create a primitive". IGC 2.35.5 fixes it (it is also what the LLM 14B needs,
#     so one IGC now serves BOTH GPU services - the old 2.23-vs-2.35 conflict is gone).
#   - 512x512 works on older IGC; 768/1024 need 2.35.5.
#
# transformers MUST be <5: transformers 5.x changed CLIPTextModel and breaks diffusers SDXL
#   ("'CLIPTextModel' object has no attribute 'text_model'").

diffusers>=0.38.0
transformers>=4.40.0,<5
accelerate>=0.28.0
safetensors>=0.4.0
pillow
# Text-to-video (videogeni — Wan2.1/LTX/CogVideoX via diffusers, same venv as image gen):
#   sentencepiece is REQUIRED — the T5/umT5 text-encoder tokenizer won't load without it.
#   ftfy is optional prompt cleanup. (protobuf is already pulled in transitively.)
sentencepiece
ftfy
# Image editing (regeni — text-grounded auto-mask + SDXL inpaint, same venv as image gen):
#   NO new pip deps — CLIPSeg is a `transformers` model (CLIPSegForImageSegmentation) and inpaint is
#   diffusers' StableDiffusionXLInpaintPipeline; both are covered by the diffusers/transformers above.
#   It reuses the geni SDXL checkpoints (image_model_path / image_anime_model_path) — no edit model to
#   download except the small ~1.2GB CLIPSeg model (CIDAS/clipseg-rd64-refined, auto-downloads on
#   first use; `./install.sh --regeni` can prefetch it). Portable: stock diffusers/transformers + SDPA,
#   runs on CUDA / Intel XPU / AMD ROCm. NO flash-attn/fp8/GGUF.
