# Image generation dependencies (for venv-xpu / PyTorch XPU)
# Install in addition to requirements.txt for image-capable instances

# Diffusers for native image generation
diffusers>=0.28.0
transformers>=4.40.0
accelerate>=0.28.0
safetensors>=0.4.0

# Note: torch and torchvision must be installed separately for XPU:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/test/xpu
