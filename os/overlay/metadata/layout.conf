masters = gentoo
thin-manifests = true
sign-manifests = false
# Ebuilds here are binaries and config; nothing is compiled, so a manifest of upstream tarballs is
# the only integrity this repo carries — and that is what thin-manifests records.
manifest-hashes = BLAKE2B SHA512
