# Capacitor / Node build artifacts — regenerated by CI (build-www.sh + cap sync + gradle)
node_modules/
www/
android/app/src/main/assets/public/
android/app/build/
android/build/
android/.gradle/
android/captures/
android/local.properties
*.keystore
release.keystore

# committed self-sign key (sideload) — CI signs with it, no secrets needed
!android/posterchan-release.keystore
