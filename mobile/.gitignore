# Capacitor / Node build artifacts — regenerated by CI (build-www.sh + cap sync + gradle)
node_modules/
www/
android/app/src/main/assets/public/
android/app/build/
android/build/
android/.gradle/
android/captures/
android/local.properties
*.keystore
*.jks
*.p12
*.pfx
release.keystore

# Signing keys are never source files. CI reconstructs its keystore from an encrypted secret.
